// Node has data and a yes (left) and no (right) answer
class BinaryNode {
    constructor(data, yesNode, noNode) {
        this.data = data;
        this.yesNode = yesNode;
        this.noNode = noNode;
    }
    
}

export default BinaryNode;